#version 330 // OpenGL version 3.3 (minimum requirement)

in vec3 inPosition;
in vec4 inColor; 

uniform mat4 WVP; 

out vec4 fragColor;

void main()
{
    gl_Position = WVP * vec4(inPosition, 1.0);
    fragColor = inColor;
}