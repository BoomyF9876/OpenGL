#version 330 // OpenGL version 3.3 (minimum requirement)

in vec3 retColor; // Input variable from vertex shader
in vec2 retTexCoord; // Output variable for final color

out vec4 FragColor;
uniform sampler2D texSampler;

void main()
{
    FragColor = texture(texSampler, retTexCoord);
}